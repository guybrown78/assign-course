import { Registration } from "./Registration";

export class User {
  id: string;
  firstName: string;
  lastName: string;
  registrationCount: number;
  //registrations: Registration[];
  totalCourses: number;
}
